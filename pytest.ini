[pytest]
markers =
    visual: Headless Chrome screenshot tests
    integration: Tests requiring running services (gateway, news API)
    nightly: Expensive tests using LLM tokens
addopts = -p no:playwright
