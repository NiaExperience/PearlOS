# Example allowlist file for `npm run allowlist:import`.
#
# One email per line. Blank lines and lines starting with "#" are ignored.
# Commas or whitespace also work as separators on a single line.
#
# Usage:
#   npm run allowlist:import -- path/to/this/file.txt
#
# Or with the seeder directly:
#   npm run allowlist:seed -- --from-file path/to/this/file.txt
#   npm run allowlist:seed -- --from-file path/to/this/file.txt --replace
#   npm run allowlist:seed -- --from-file path/to/this/file.txt --dry-run

# Engineering
cr7himanshuyadav@gmail.com
niteshkr50@gmail.com
stu2003235@gmail.com

# Multiple on one line is fine too:
# user-a@example.com, user-b@example.com
