# Claude-Side QA Audit - Staging - 2026-05-24 03:03 UTC

Scope: read-only audit of staging deployment at `https://134-209-76-227.sslip.io/`.

Note: this Codex sandbox has restricted network and PID namespace access. Direct `curl` to the staging URL and localhost ports failed from inside the sandbox, and `pm2 list` failed on PM2 socket access. I used readable source/deploy files, PM2 dump/log files, and PM2 pid/log artifacts as the audit source where direct process/network checks were blocked.

## Executive Summary

Staging is not running from the expected interface deploy cwd. PM2 dump shows `interface` is online but running from:

```text
/workspace/nia-universal/apps/interface
```

The handoff expectation says staging interface should run from:

```text
/home/deploy/pearlos/apps/interface
```

That cwd mismatch is the main root cause of the reported build-name problem. The deploy source tree has newer Settings UI code that fetches `/api/health/build`, but the running source tree and running `.next` bundle still contain old `VISUAL SILVER` code.

## Part 1 - Build Name Audit

### Source build-info.json

`/workspace/nia-universal/apps/interface/src/build-info.json`:

```json
{
  "buildId": "ccccb236",
  "buildTime": "2026-05-23T20:17:26.763Z",
  "codename": "GLIMMER POND"
}
```

### Deploy build-info.json

`/home/deploy/pearlos/apps/interface/src/build-info.json`:

```json
{
  "buildId": "c1909234",
  "buildTime": "2026-05-23T01:05:48.007Z",
  "codename": "GLIMMER POND"
}
```

Both source and deploy `build-info.json` files say `GLIMMER POND`, but they have different build IDs/times.

### Deploy health/build route source

`/home/deploy/pearlos/apps/interface/src/app/api/health/build/route.ts` reads from:

```ts
path.join(process.cwd(), 'src', 'build-info.json')
```

It sets `codename = parsed.codename ?? codename`, so in deploy source it should return `GLIMMER POND` if the process cwd is `/home/deploy/pearlos/apps/interface`.

### Running/source route and bundle mismatch

`/workspace/nia-universal/apps/interface/src/app/api/health/build/route.ts` has:

```ts
const BUILD_CODENAME = 'GLIMMER POND';
```

However, the running source-tree build artifact still has old compiled output:

```text
/workspace/nia-universal/apps/interface/.next/server/app/api/health/build/route.js
return B={codename:"VISUAL SILVER", ...}
```

The deploy-tree `.next` artifact also contains old compiled output:

```text
/home/deploy/pearlos/apps/interface/.next/server/app/api/health/build/route.js
return B={codename:"VISUAL SILVER", ...}
```

### Settings UI mismatch

The source tree still hardcodes the old UI fallback:

```ts
// /workspace/nia-universal/apps/interface/src/components/settings-panels/SettingsPanels.tsx
const BUILD_KEYWORD = 'VISUAL SILVER';
const DEFAULT_BUILD_INFO = { buildName: `PearlOS ${BUILD_KEYWORD}` };
const buildInfo = DEFAULT_BUILD_INFO;
```

The deploy tree has newer code:

```ts
// /home/deploy/pearlos/apps/interface/src/components/settings-panels/SettingsPanels.tsx
const [buildInfo, setBuildInfo] = useState<BuildInfo>(DEFAULT_BUILD_INFO);
fetch('/api/health/build', { cache: 'no-store' });
buildName: `PearlOS ${codename}`;
```

But PM2 is not using that deploy cwd.

### Live API check

Attempted:

```bash
curl -sk --max-time 15 https://134-209-76-227.sslip.io/api/health/build
curl -sk --resolve 134-209-76-227.sslip.io:443:134.209.76.227 https://134-209-76-227.sslip.io/api/health/build
```

Both failed from this sandbox with curl exit codes `6`/`7` and no response body, consistent with restricted network access here. The compiled route evidence above strongly predicts the live route will return `VISUAL SILVER` until the app is rebuilt and restarted from the intended cwd.

### PM2 cwd

From `/home/deploy/.pm2/dump.pm2`, `interface`:

```json
{
  "name": "interface",
  "status": "online",
  "cwd": "/workspace/nia-universal/apps/interface",
  "args": [
    "-lc",
    "set -a; [ -f /workspace/nia-universal/apps/interface/.env.local ] && . /workspace/nia-universal/apps/interface/.env.local; unset SLACK_BOT_TOKEN SLACK_APP_TOKEN; set +a; NODE_ENV=production PORT=3000 npm start"
  ],
  "restart_time": 3669
}
```

Expected per task: `/home/deploy/pearlos`.

### Root cause

There are two overlapping causes:

1. `interface` PM2 is running from `/workspace/nia-universal/apps/interface`, not `/home/deploy/pearlos/apps/interface`.
2. The `.next` bundles in both source and deploy trees are stale and still compile `/api/health/build` with `codename:"VISUAL SILVER"`.

Deploy-tree source has the newer dynamic Settings UI, but the running process does not use that tree. Source-tree route source says `GLIMMER POND`, but the source-tree build artifact was not rebuilt after that source change.

## Part 2 - Feature Integration Audit

### A. Voice task identity fix

Present in deploy tree.

`/home/deploy/pearlos/apps/pipecat-daily-bot/bot/bot_gateway.py`:

```text
3559:def _enrich_voice_identity_from_request(...)
4212:    _enrich_voice_identity_from_request(body, request, req_logger)
```

The function enriches `sessionUserId`, `sessionUserEmail`, and `sessionUserName` from signed claims and forwarded user headers before launch.

`/home/deploy/pearlos/apps/pipecat-daily-bot/bot/tools/openclaw_tools.py`:

```text
117:def _identity_from_daily_participants()
269:        fallback_identity = _identity_from_daily_participants()
```

It falls back to live Daily participant metadata when env identity is missing.

`/home/deploy/pearlos/apps/pipecat-daily-bot/bot/bot_queue_worker.py`:

```text
39:async def hydrate_payload_identity(r: redis.Redis, payload: dict) -> None:
140:                await hydrate_payload_identity(r, payload)
```

It hydrates queued launch identity from Redis session state.

Assessment: deployed in files. Runtime likely uses this because PM2 cwd for `pipecat-gateway`, `pipecat-runner`, and `bot-queue-worker` is `/home/deploy/pearlos/apps/pipecat-daily-bot/bot`.

### B. Star Office iframe integration

Present in deploy tree.

`/home/deploy/pearlos/apps/interface/src/features/ManeuverableWindow/components/ContentPanel.tsx`:

```tsx
const PearlHouseContent: React.FC = () => (
  <iframe
    src="/pearls-house"
    title="Pearl's House"
    ...
  />
);
...
if (win.viewType === "pearlHouse") {
  return <PearlHouseContent />;
}
```

`/home/deploy/pearlos/apps/interface/src/components/browser-window.tsx`:

```text
case 'pearls-house':
case 'star-office':
  requestWindowOpen({ viewType: 'pearlHouse', ... })
...
case 'pearlHouse':
  <iframe src="/pearls-house" title="Pearl's House" ... />
```

`/home/deploy/pearlos/apps/interface/src/middleware.ts` includes:

```ts
'/pearls-house',
```

in `PUBLIC_ROUTES`.

Assessment: source integration is present in deploy tree. Whether users see it depends on rebuilding/restarting the interface from the correct tree, because PM2 is currently running the source tree.

### C. Star Office state sync

Requested deploy path:

```text
/home/deploy/pearlos/scripts/sync-star-office-state.sh
```

Result: missing.

Source path exists:

```text
/workspace/nia-universal/scripts/sync-star-office-state.sh
```

The script writes to `${ROOT_DIR}/apps/star-office/state.json` and `${ROOT_DIR}/apps/star-office/agents-state.json`, defaulting `ROOT_DIR` to `/workspace/nia-universal`.

Requested deploy state files:

```text
/home/deploy/pearlos/apps/star-office/state.json
/home/deploy/pearlos/apps/star-office/agents-state.json
```

Result: `/home/deploy/pearlos/apps/star-office` does not exist.

Actual PM2 `pearls-house` cwd:

```json
{
  "name": "pearls-house",
  "status": "online",
  "cwd": "/workspace/nia-universal/apps/star-office",
  "script": "/workspace/nia-universal/apps/star-office/pearls_house_server.py"
}
```

Actual source state:

`/workspace/nia-universal/apps/star-office/state.json`:

```json
{
  "state": "syncing",
  "detail": "Pearl has 1 queued task(s)",
  "progress": 20,
  "updated_at": "2026-05-24T03:01:05Z",
  "pearl_status": "working",
  "ttl_seconds": 300,
  "source": "pearlos-openclaw",
  "activity": {
    "assignee": "claude_cli",
    "in_progress": 0,
    "pending": 1,
    "worker_online": "yes"
  }
}
```

`/workspace/nia-universal/apps/star-office/agents-state.json` has a Pearl entry:

```json
{
  "agentId": "pearl",
  "name": "Pearl",
  "isMain": true,
  "state": "syncing",
  "detail": "Pearl has 1 queued task(s)",
  "pearl_status": "working"
}
```

Cron search:

```text
No star-office/sync-star/state cron entry found in current crontab, /etc/cron*, or /var/spool/cron.
```

Assessment: state sync exists and is producing current-looking source-tree state, but it is not deployed at the requested deploy path and no cron job was found. It was likely run manually or by another scheduler not visible in cron.

## Part 3 - Service Health

### PM2 services from dump

Online:

```text
mesh
pipecat-gateway
pipecat-runner
bot-queue-worker
openclaw-bridge
deepseek-proxy
pearl-worker
pearl-agent-runtime
discord-silence-watchdog
openclaw-gateway
interface
pearls-house
the-agency-office
pearl-chat-relays-production-repair
```

Stopped:

```text
webchat-team
```

### PocketTTS on 8766

No PM2 dump entry for `pocket-tts` is present, but log files exist.

`/home/deploy/.pm2/logs/pocket-tts-out.log` shows historical successful health checks:

```text
Uvicorn running on http://127.0.0.1:8766
GET /health HTTP/1.1" 200 OK
POST /tts HTTP/1.1" 200 OK
```

Direct `curl http://127.0.0.1:8766/health` failed from this sandbox because localhost is inside the sandbox network namespace, not host PM2 networking. Based on PM2 dump, PocketTTS is not currently managed by PM2.

Assessment: cannot prove currently running from inside this sandbox; PM2 dump does not show it. Last log evidence shows it has worked before.

### OpenClaw gateway on 18789

PM2 dump says `openclaw-gateway` is online, cwd `/home/deploy/OpenClaw`, restart_time `4`.

Recent logs show repeated config warnings at `2026-05-24T03:05-03:06Z`, which means the process is active:

```text
Config warnings:
- plugins.entries.anthropic: plugin id mismatch
- plugins.entries.brave: plugin id mismatch
- plugins.entries.deepseek: plugin id mismatch
- plugins.entries.moonshot: plugin id mismatch
- plugins.entries.openrouter: plugin id mismatch
```

Direct `curl http://127.0.0.1:18789/health` failed from sandbox network isolation.

### Pipecat gateway on 4444

PM2 dump says `pipecat-gateway` is online, cwd `/home/deploy/pearlos/apps/pipecat-daily-bot/bot`, restart_time `11`.

Recent logs at `2026-05-24T03:03-03:06Z` show active authenticated traffic:

```text
[pipecat] [auth] path=/api/tasks required=1 header_present=1 allowed_secret_count=1 result=allow
[pipecat] [auth] path=/api/chat/inbox required=1 header_present=1 allowed_secret_count=1 result=allow
```

Direct `curl http://127.0.0.1:4444/health` failed from sandbox network isolation.

## Part 4 - Gap Analysis

### What is missing from staging

1. Interface is not running from the expected deploy tree.
   - Expected: `/home/deploy/pearlos/apps/interface`
   - Actual PM2 cwd: `/workspace/nia-universal/apps/interface`

2. Interface build artifacts are stale.
   - Source `build-info.json`: `GLIMMER POND`
   - Source route source: `GLIMMER POND`
   - Running `.next` route bundle: `VISUAL SILVER`
   - Deploy `.next` route bundle: `VISUAL SILVER`

3. Deploy tree contains newer Settings UI, but running tree does not.
   - Deploy Settings fetches `/api/health/build`.
   - Source/running Settings hardcodes `VISUAL SILVER`.

4. Star Office deploy path is incomplete.
   - `/home/deploy/pearlos/apps/star-office` is missing.
   - `/home/deploy/pearlos/scripts/sync-star-office-state.sh` is missing.
   - PM2 `pearls-house` runs from `/workspace/nia-universal/apps/star-office`.

5. No cron job was found for Star Office state sync.

6. `webchat-team` is stopped in PM2 dump.

7. PocketTTS is not present as a PM2-managed service in the dump, despite old logs showing it served health checks on `8766`.

### Studio play button connected to launch?

The deployed Studio view is read-only visualization:

`/home/deploy/pearlos/apps/interface/src/features/TaskVisualization/StudioView.tsx` renders:

- lanes
- orchestration map
- active work
- recent creations

No play button, launch handler, or run/start action is present in this component. Searches found Studio window routing, but not a Studio play-to-launch implementation.

Assessment: not connected in the deployed `StudioView.tsx`.

### Error logs in the last hour

Log scan for `2026-05-24T02:` and `2026-05-24T03:` found:

```text
pipecat-gateway-error.log 492
pearl-agent-runtime-out.log 263
discord-silence-watchdog-out.log 198
openclaw-gateway-error.log 178
pearl-worker-out.log 72
interface-out.log 40
pearl-chat-relays-production-repair-out.log 26
interface-error.log 3
```

Notable recent errors/warnings:

```text
interface-error.log:
[getCallObject] Failed to create DailyIframe call object
meta.error="WebRTC not supported or suppressed"
timestamps: 2026-05-24T02:46:34.700Z and 2026-05-24T03:01:56.723Z
```

```text
interface-error.log:
Failed to find Server Action "x". This request might be from an older or newer deployment.
```

```text
openclaw-gateway-error.log:
Config warnings for plugin id mismatches and disabled plugin config.
```

```text
pearl-chat-relays-production-repair-out.log:
task follow-up routed to inbox {"status":"failed","terminal":true}
```

### Interface restart count 3669

PM2 dump:

```json
{
  "name": "interface",
  "status": "online",
  "restart_time": 3669,
  "unstable_restarts": 0
}
```

PM2 log shows recent manual/controlled restarts:

```text
2026-05-23T21:28:00: Stopping app:interface
2026-05-23T21:28:00: App [interface:11] online
2026-05-24T03:00:56: Stopping app:interface
2026-05-24T03:00:56: App [interface:11] online
```

The current logs do not show a crash loop in the last sampled period. `unstable_restarts` is `0`. The large `restart_time` appears historical, not currently increasing from crashes in the sampled logs. The old Server Action errors are consistent with stale client/server build mismatch after deploys or cached pages.

## Recommended Fix Order

1. Reconcile source/deploy drift for interface files.
   - Deploy tree has newer `SettingsPanels.tsx` than source tree.
   - Source is supposed to be truth, so sync the deploy-only Settings change back into `/workspace/nia-universal` before rebuilding.

2. Rebuild interface from the intended source.
   - `npm run build --prefix apps/interface`

3. Make PM2 `interface` run from the expected deploy cwd, or update the staging handoff if the desired runtime cwd has changed.
   - Current cwd violates the stated expectation.

4. Copy built/source artifacts forward consistently.
   - Ensure `/home/deploy/pearlos/apps/interface/src/app/api/health/build/route.ts`, `src/build-info.json`, `src/components/settings-panels/SettingsPanels.tsx`, and `.next` agree.

5. Restart interface and re-check `/api/health/build`.
   - Expected codename: `GLIMMER POND`.

6. Deploy `scripts/sync-star-office-state.sh` to `/home/deploy/pearlos/scripts/` if the deploy tree is expected to hold runtime scripts.

7. Decide whether Star Office should run from `/workspace/nia-universal/apps/star-office` or `/home/deploy/pearlos/apps/star-office`, then make PM2 cwd, state file paths, and docs agree.

8. Add a cron/systemd timer for Star Office state sync if live state is expected to update automatically.

9. Verify PocketTTS outside this sandbox on the host network, since PM2 dump does not show a managed service for it.

